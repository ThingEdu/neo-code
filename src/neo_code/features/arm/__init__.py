"""Robotic arm feature — Qt-clean logic for Chơi mode.

`protocol` and `student_api` are imported by the student's subprocess, so
neither may import Qt. `session` is the main-process side and does use Qt.
"""
