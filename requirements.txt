# NEO Code runtime dependency.
# On the NEO One (Armbian/ARM) and Debian, PyQt6 and the Qt6 QML runtime come
# from apt via the .deb package — never build PyQt6 from pip on ARM.
# This file is for x86 dev environments where pip wheels are available.
PyQt6>=6.4,<7
